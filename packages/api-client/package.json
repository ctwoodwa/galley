{
  "name": "@galley/api-client",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Typed clients for SPA → Windows GPU inference API (TTS, STT, image, music)",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^6.0.3",
    "vitest": "^4.1.5"
  },
  "dependencies": {
    "zod": "^4.4.3"
  }
}
