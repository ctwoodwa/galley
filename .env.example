# Book content repo path (used by book-server)
BOOK_ROOT=/path/to/the-inverted-stack

# Optional: override where the built SPA is served from
# WEB_DIST=/path/to/galley/apps/web/dist

# API server port
PORT=3080
