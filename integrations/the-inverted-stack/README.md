# Integration: the-inverted-stack

Adapter config linking the Galley book-server to the *The Inverted Stack* content repo.

## Setup

```bash
cp sync.config.example.json sync.config.json
# Edit sync.config.json and set bookRoot to your local repo path
```

`sync.config.json` is gitignored (contains absolute local path).
