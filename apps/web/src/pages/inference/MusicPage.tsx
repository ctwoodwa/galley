/**
 * /inference/music — raw music-library exploration.
 * Phase 3b will replace this stub with the ported MusicPanel from inference-studio.
 */
export default function MusicPage() {
  return (
    <div className="p-8">
      <h2 className="text-lg font-semibold text-text mb-2">Music</h2>
      <p className="text-sm text-text-muted">
        Music library + ingestion. Coming in Phase 3b — port of <code>MusicPanel</code>.
      </p>
    </div>
  )
}
