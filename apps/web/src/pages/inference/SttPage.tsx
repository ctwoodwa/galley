/**
 * /inference/stt — raw STT exploration.
 * Phase 3b will replace this stub with the ported STTPanel from inference-studio.
 */
export default function SttPage() {
  return (
    <div className="p-8">
      <h2 className="text-lg font-semibold text-text mb-2">STT</h2>
      <p className="text-sm text-text-muted">
        Raw speech-to-text API exploration. Coming in Phase 3b — port of <code>STTPanel</code>.
      </p>
    </div>
  )
}
