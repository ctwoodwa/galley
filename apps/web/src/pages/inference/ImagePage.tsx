/**
 * /inference/image — raw image-generation exploration.
 * Phase 3b will replace this stub with the ported ImagePanel from inference-studio.
 */
export default function ImagePage() {
  return (
    <div className="p-8">
      <h2 className="text-lg font-semibold text-text mb-2">Image</h2>
      <p className="text-sm text-text-muted">
        Raw image-generation API exploration. Coming in Phase 3b — port of <code>ImagePanel</code>.
      </p>
    </div>
  )
}
