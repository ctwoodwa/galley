/**
 * /inference/voices — raw TTS exploration.
 * Phase 3b will replace this stub with the ported TTSPanel from inference-studio.
 */
export default function VoicesPage() {
  return (
    <div className="p-8">
      <h2 className="text-lg font-semibold text-text mb-2">Voices (TTS)</h2>
      <p className="text-sm text-text-muted">
        Raw TTS API exploration. Coming in Phase 3b — port of <code>TTSPanel</code> from inference-studio.
      </p>
    </div>
  )
}
